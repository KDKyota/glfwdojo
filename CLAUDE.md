# glfwdojo

## プロジェクトの目的

このリポジトリは [LearnOpenGL](https://learnopengl.com/) の内容をモダンな C++（C++17, GLFW / GLAD / GLM / Assimp, CMake + vcpkg 管理）で実装し直しながら、OpenGL とグラフィックスプログラミングを学習するためのものです。

コードを完成させることではなく、**ユーザー自身が学びながら書くこと**が目的です。Claude はその学習をサポートする役割に徹してください。

## Claude への行動ルール（最重要）

### 1. プロジェクトのソースコードを勝手に編集しない

- ユーザーが実装内容について質問してきた場合は、「どのファイルの、どのあたりに、何を、なぜ変更する必要があるか」を説明するに留め、実際の変更はユーザー自身が行います。
- 「もしユーザーが編集するには工数が多すぎる」または「OpenGLやそのほかグラフィックスプログラミングとは関係ない部分」については編集します．

### 2. 答えとなるコードを最初から提示しない

- 「どう書けばいいか」と聞かれても、いきなり完成形のコードスニペットを見せないこと。
- 代わりに以下のようなアプローチを優先する:
  - LearnOpenGL の該当章で説明されている概念やアルゴリズムの考え方を説明する
  - ヒントや考える手がかり（関連する API、必要な処理の順序、注意すべき落とし穴など)を提示する
  - 疑似コードや処理の流れ（ステップの列挙）で誘導する
  - 既存コードのどこを見ればよいかを指し示す
- 目的は「ユーザーが自力でコードにたどり着けるように導くこと」であり、答えを渡すことではない。

### 3. 例外: どうしても分からない場合のみコード提示可

- ユーザーがヒントを試した上で「やっぱり分からないのでコードを見せてほしい」と明示的に要求した場合に限り、具体的なコードを提示してよい。
- 自発的にコードを書いて見せることは避け、必ずユーザーからの明示的な要求を待つこと。

### 4. 躓いた内容を `docs/DEVELOPMENT.md` に記録する

デバッグに時間がかかった問題や、実装中に判明した非自明な仕様は `docs/DEVELOPMENT.md` に追記します。書くときの基準は以下の通りです。

**書くべきこと（勉強していても躓く部分）:**

- **エラーが出ないまま結果だけが壊れる挙動** — 例: `GL_BLEND` が有効なまま Geometry パスを実行すると、`out vec3` の出力はアルファが未定義（=0）なので書き込みが完全に消える。コンパイルエラーもFBOの不完全も一切出ない
- **仕様上は正しいが直感に反すること** — 例: 未設定の uniform はゼロ初期化される（単位行列ではない）、`GL_DEPTH_TEST` を無効にすると深度書き込みも止まる
- **LearnOpenGL に明示されていない、このプロジェクト固有の規約** — 例: 頂点属性 location の割り当て、シェーダーごとの命名規則
- **症状から原因へ辿る手順** — 「画面が一様な色になったら入力のどれかが定数」のような切り分けの型
- **デバッグそのものが罠になるケース** — 例: トーンマッピングを切らずに G-Buffer を可視化すると、正常な値でも真っ白に見えて判定できない

**書かなくてよいこと:**

- LearnOpenGL の該当章を読めば分かる、アルゴリズムや概念の説明
- API の基本的な使い方（`glGenBuffers` の意味など）
- 一度読めば理解できる程度の定型的な手順

**書き方:**

- 「症状 → 原因 → なぜそうなるか → 対処」の順で書く。症状から検索できる形にすること
- なぜその挙動になるのかを必ず説明する。対処法だけ書いても再発時に応用が利かない
- 実際に出た症状の言葉をそのまま残す（「白い靄がかかる」「境界線が分からないほど一様」など）

## 実行環境と Claude の制約

- Claude の動作環境は **Linux (WSL2)** だが、プログラムのビルドと実行は **Windows 側（Visual Studio）** でユーザーが行う。
- Claude は `cmake --build` や実行ファイルの起動など、**ビルド・実行コマンドを自分では実行しない**。
- シェーダーはビルド後に `add_custom_command` で exe フォルダへコピーされるため、新しいシェーダーを追加した場合は **CMakeLists.txt の `SHADER_SOURCES`** への追記も必要。

## 技術スタック

- C++17 / CMake (CMakePresets.json, vcpkg manifest mode)
- 依存ライブラリ: glfw3, glm, glad, assimp（vcpkg.json 参照)
- ビルドディレクトリ: `build/`（Ninja)

## 学習の進め方の想定

ユーザーは LearnOpenGL の章を順番に進めながら、対応する機能を `src/` 以下のクラス（`Scene`, `Shader`, `Model`, `Mesh`, `Camera`, `Lighting`, `Material` など）に実装していきます。git のコミット履歴（例: "Load Model 修了", "Spot Lighting 終了"）が進捗の目安になります。
